from Object import *

"""
class Enemy (Object):
    def __init__(self):
        self.imageIndex = 0
        self.sprite = pygame.image.load(enemyImage[imageIndex])
        self.rect = self.sprite.get_rect()
        self.xpos = random.randrange(-self.cloudRect.width, scr_width-self.cloudRect.width)
        self.ypos = random.randrange(-100, -10)
        self.speed = random.randrange(1, 2)
        self.sizeX = 100

    def update(self):
        if self.xpos > scr_width:
            self.xpos = -self.cloudRect.width
        self.xpos += self.speed

    def draw(self):
        screen.blit(self.sprite, (self.xpos, self.ypos))
"""
